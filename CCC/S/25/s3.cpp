#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n, m, q;
    cin >> n >> m >> q;

    vector<int> pen_colour(n);
    vector<int> pen_pretty(n);

    for (int i = 0; i < n; i++){
        cin >> pen_colour[i] >> pen_pretty[i];
    }
}