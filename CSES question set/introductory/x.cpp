#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int mod = 1e9 + 7;
int inf = 1e9;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int n, m;
    cin >> n >> m;
    vector<vector<int>> (n,vector<int>(n));
    
}